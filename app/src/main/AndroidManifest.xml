<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="erim.test"
    android:installLocation="preferExternal">
    <!--Install location on SD card-->

    <!--use by webview-->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:hardwareAccelerated="true">
        <activity
            android:name=".Sommaire"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UriWeb" />
        <activity android:name=".Web" />
        <activity android:name=".HelloWolrd" />
        <activity android:name=".Countdown" />
        <activity android:name=".Notif"/>
        <activity android:name=".Countdown_2"/>
        <activity android:name=".Bouton"/>
    </application>

</manifest>
